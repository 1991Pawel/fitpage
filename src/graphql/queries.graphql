query GetPostBySlug($slug: String!) {
	post(where: { slug: $slug }) {
		id
		title
		tags
		publishedAt
		blogContent {
			html
		}
	}
}
query GetAllPosts {
	posts {
		title
		tags
		id
		slug
		coverImage {
			id
			coverImagePost {
				coverImage {
					fileName
					url
				}
			}
		}
	}
}
